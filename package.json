{
  "name": "yumbot",
  "private": true,
  "version": "1.0.0",
  "packageManager": "pnpm@9.0.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently \"pnpm --filter api dev\" \"pnpm --filter mini-app dev\" \"pnpm --filter web dev\"",
    "dev:api": "pnpm --filter api dev",
    "build": "pnpm --filter remnawave build && pnpm --filter api build && pnpm --filter mini-app build && pnpm --filter web build",
    "db:generate": "pnpm --filter api db:generate",
    "db:migrate": "pnpm --filter api db:migrate",
    "db:studio": "pnpm --filter api db:studio"
  },
  "devDependencies": {
    "concurrently": "^9.0.0",
    "typescript": "^5.5.0"
  }
}
